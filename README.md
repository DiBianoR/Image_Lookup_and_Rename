# Image_Lookup_and_Rename
Does a reverse image search and tries to find the artist title and year of a work and set the file name accordingly

It requires a SerpAPI key (for the google lens search) and a Google API key (for the followup interpretation)
It also requires the images in question be accessible via a public url. I use Abyss web server for this.
